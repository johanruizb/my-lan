# Tests de integración

Pruebas end-to-end de MyLAN con base de datos temporal y fixtures de red.

Se añaden a partir del Paso 5 (pipeline scan → enrichment → persist → export) y del
Paso 8 (integración completa). En este push (Paso 1) solo existe este placeholder para
fijar la estructura del directorio.
